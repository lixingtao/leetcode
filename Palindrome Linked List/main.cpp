#include <vector>
#include <map>
#include <cstring>
#include <cstdlb>
#include <cstdio>
#include <iostream>
#include <algorithm>
#include <set>

using namespace std;

struct ListNode {
     int val;
     ListNode *next;
     ListNode(int x) : val(x), next(NULL) {}
};

bool isPalindrome(ListNode* head) {
    int len = 0;
    ListNode *p = head;
    while(p) {
        len++;

    }
}

int main()
{
    return 0;
}
